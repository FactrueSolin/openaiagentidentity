# Web listener. Process environment variables override these values.
HOST=127.0.0.1
PORT=3000

# Optional HTTP/HTTPS proxy for every outbound request made by the web server.
# Leave empty for a direct connection.
PROXY_URL=
